class Schedule {
    constructor(name, session, time){
        this.name = name;
        this.session = [];
        this.time = datetime-local;
    }
}

class ScheduleUpdate {
    static url = 'https://SECRET.mockapi.io/:endpoint';

    static getAllSchedules() {
        return $.get(this.url);
    }

    
}